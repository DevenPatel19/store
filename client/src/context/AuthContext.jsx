import { createContext  } from "react";

// Separation of concerns => Context Definition Only -- Just don't have any manipulation on this level
// Create and export the context


export const AuthContext = createContext(null);
